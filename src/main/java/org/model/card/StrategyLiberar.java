package org.model.card;

import org.services.IPeon;
import org.services.IStrategyAzar;

public class StrategyLiberar implements IStrategyAzar {

    @Override
    public void activarEfecto(IPeon jugador) {

        /*if (jugador.tieneTarjetaLiberacion()) {
            boolean liberado = jugador.usarTarjetaLiberacion();
            if (liberado) {
                System.out.println("El jugador ha sido liberado de la cárcel.");

            } else {
                System.out.println("El jugador no tiene una tarjeta de liberación válida.");
            }
        } else {
            System.out.println("El jugador no tiene la tarjeta de liberación de la cárcel.");
        }*/
    }
}
